# Commit timeline

The timeline panel owns the time-series loading state and marker configuration.
It compares additions with files changed and keeps the reusable line chart
agnostic about the dashboard's meaning.
