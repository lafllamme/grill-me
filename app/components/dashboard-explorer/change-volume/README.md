# Change volume chart

The change volume panel owns loading replay and the two-series additions /
deletions composition. Its data can later come from the normalized GitHub
commit snapshot without changing the page layout.
