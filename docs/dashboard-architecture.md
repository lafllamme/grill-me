# Dashboard Architecture

This document is retained as a compatibility link. The canonical architecture
now lives in [docs/dashboard/architecture.md](./dashboard/architecture.md).

Use the canonical document for new decisions; the old path remains so existing
links do not break.
